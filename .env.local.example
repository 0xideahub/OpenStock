# OpenStock Backend API Environment Variables
# Copy this file to .env.local and fill in your values

# Tiingo API Key (get from https://api.tiingo.com/)
TIINGO_API_KEY=your_tiingo_api_key_here

# Internal API Key for authentication between mobile app and backend
# Generate with: openssl rand -hex 32
# This protects your backend from unauthorized access
INTERNAL_API_KEY=your_internal_api_key_here

# Upstash Redis for rate limiting and caching (get from https://upstash.com/)
# Sign up for free tier at https://console.upstash.com/
# Create a Redis database and copy the REST API credentials
UPSTASH_REDIS_REST_URL=your_upstash_redis_rest_url_here
UPSTASH_REDIS_REST_TOKEN=your_upstash_redis_rest_token_here

# OpenAI API key for AI-generated analysis (optional but required for growth analysis)
OPENAI_API_KEY=your_openai_api_key_here
# Optional: override default model (defaults to gpt-4o-mini)
# OPENAI_MODEL=gpt-4o-mini

# Rate Limiting Configuration (optional - defaults shown)
# RATE_LIMIT_REQUESTS=60          # Max requests per window
# RATE_LIMIT_WINDOW=60 s          # Time window (e.g., "60 s", "1 m", "1 h")
